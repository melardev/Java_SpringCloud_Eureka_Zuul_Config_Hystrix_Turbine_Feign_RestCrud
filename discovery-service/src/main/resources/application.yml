server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    fetch-registry: false
    register-with-eureka: true


management:
  endpoints:
    web:
      exposure:
        include: '*'